<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         colors="true"
         backupGlobals="true"
         stopOnFailure="false"
         processIsolation="false"
         bootstrap="tests/bootstrap.php"
         cacheDirectory="vendor/.cache.phpunit"
         backupStaticProperties="false"
         failOnEmptyTestSuite="false"
>
    <testsuites>
        <testsuite name="unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="integration">
            <directory>tests/Integration</directory>
        </testsuite>
        <testsuite name="feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>

    <coverage/>

    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>

    <!--
        Configuration for the LIVE AI provider tests lives in `tests/.env`
        (gitignored; copy `tests/.env.sample`). tests/bootstrap.php loads it via
        symfony/dotenv. Without it those tests skip and the suite stays green.
        See tests/README.md.
    -->
    <php>
        <ini name="error_reporting" value="-1"/>
        <ini name="memory_limit" value="-1"/>
    </php>
</phpunit>
